<h1>Teams Template</h1>
<table class="table table-striped">
    <thead>
    <tr>
        <th>Name</th>
        <th>picture</th>
        <th>founded</th>
    </tr>
    </thead>
    <tbody>
    {{#each team in model}}
        <tr>
            <td>{{team.name}}</td>
            <td>{{team.picture}}</td>
            <td>{{team.creation_date}}</td>
        </tr>
    {{/each}}
    </tbody>
</table>

{{outlet}}
